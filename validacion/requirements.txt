# ────────── Núcleo de manipulación de datos ──────────
pandas>=2.2.1           # lectura CSV/JSON, joins, export
numpy>=1.26.4           # base numérica

# ────────── Métricas de claridad (RQ5) ──────────
textstat>=0.7.3         # Flesch, FK, LIX, etc.
sentence-transformers>=2.6.1   # embeddings all-mpnet-base-v2
scipy>=1.12.0           # t-test, z-score / stats
nltk>=3.8.1             # procesamiento de texto, índices de legibilidad
spacy>=3.7.4            # análisis de texto, detección de términos ambiguos

# ────────── Conversión a catálogo SIREN ──────────
jinja2>=3.1.3           # plantillas CSV/Markdown

# ────────── Vector-DB opcional (para RAG ligero) ──────────
faiss-cpu>=1.7.4        # índice FAISS; si vas a usar GPU pon faiss-gpu
chromadb>=0.4.24        # alternativa sencilla a FAISS (opcional)

# ────────── Utilidades varias ──────────
scikit-learn>=1.4.2     # cosine_similarity, PCA… (usado en algunos scripts)
python-dotenv>=1.0.1    # cargar tokens/API keys desde .env (opcional)

# ────────── Compatibilidad con Trust4AI (cuando lleguemos a RQ1) ──────────
torch>=2.2.2            # backend de sentence-transformers y GENIE
# Trust4AI libs no se instalan aún – se añadirán cuando saltemos a RQ1.
matplotlib

